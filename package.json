{
  "name": "liquidbot",
  "version": "0.1.0",
  "private": true,
  "description": "LiquidBot - Aave V3 Base Liquidation Protection Service",
  "license": "MIT",
  "workspaces": [
    "backend",
    "contracts"
  ],
  "scripts": {
    "contracts:build": "cd contracts && npm run build",
    "contracts:test": "cd contracts && npm run test",
    "contracts:test:fork": "cd contracts && npm run test:fork",
    "test": "cd backend && npm test",
    "test:all": "npm run contracts:test && npm run test",
    "e2e:local": "cd contracts && npm run e2e:local",
    "e2e:fork": "cd contracts && npm run e2e:fork",
    "e2e:backend": "cd contracts && npm run e2e:backend",
    "deploy:executor": "cd contracts && npm run deploy:executor",
    "verify:executor": "cd contracts && npm run verify:executor"
  }
}
