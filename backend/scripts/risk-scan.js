#!/usr/bin/env node
/**
 * DEPRECATED: This script is deprecated. Use risk-scan.ts instead.
 * 
 * This file imported from dist/ which can cause stale build issues.
 * The new TypeScript version imports directly from src/ to avoid this problem.
 * 
 * Run: npm run risk:scan (which now executes: tsx scripts/risk-scan.ts)
 */

console.error('❌ This script is deprecated. Use: npm run risk:scan');
console.error('   The new TypeScript version (risk-scan.ts) imports from src/ to avoid stale build issues.');
process.exit(1);
